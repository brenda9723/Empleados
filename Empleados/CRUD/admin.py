from django.contrib import admin
from CRUD.models import Empleados
# Register your models here.

admin.site.register(Empleados)   #para que la clase que creaste se vea en la consola de administracion que viene por defecto